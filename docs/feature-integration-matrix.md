# Ma Trận Tích Hợp Tính Năng Hệ Thống (Feature Integration Matrix)

Tài liệu này chuẩn hóa sự liên kết xuyên suốt từ Giao diện (Màn hình/Nút bấm) → API Endpoint → Tầng Dịch vụ (Service) → Kho dữ liệu (Repository / Collection) → Điều kiện quyền hạn → Kết quả UI → Kiểm thử nghiệm thu và Sơ đồ tương ứng.

---

## Bảng Ma Trận Tích Hợp Đầy Đủ

| STT | Chức năng / Nút bấm | Màn hình / Component | API Endpoint | Tầng Dịch vụ (Service) | Repository / Collection | Điều kiện quyền (Access Guard) | UI Thành công / Thất bại | Kiểm thử liên quan | UC / SD / AD |
|---|---|---|---|---|---|---|---|---|---|
| 1 | **Tiếp tục với tư cách Khách** | `WelcomeScreen` (Web/Mobile) | `POST /api/v1/guest-sessions` | `guest_service.create_guest_session` | `guest_repo` / `guest_sessions` | Không yêu cầu (Public/Rate-limited) | **TC:** Lưu credential vào HttpOnly cookie/SecureStore, vào bản đồ khám phá.<br>**TB:** Toast lỗi, cho phép thử lại. | `test_guest_session_creation` | G01 / SD21 / AD17 |
| 2 | **Đăng ký Du khách** | `RegisterModal` (Web/Mobile) | `POST /api/v1/auth/register` | `auth_service.register_tourist` | `auth_repo` / `admin_users` | Public; role mặc định `user` | **TC:** Tự động đăng nhập, cấp token, khôi phục deep link tour.<br>**TB:** Báo lỗi email trùng hoặc mật khẩu yếu. | `test_tourist_registration` | U01 / SD21 / AD17 |
| 3 | **Hợp nhất phiên Khách (Claim)** | `AuthSuccessHook` (Web/Mobile) | `POST /api/v1/guest-sessions/claim` | `guest_service.claim_guest_session` | `guest_repo`, `trial_repo` / `guest_sessions`, `trial_usage` | `current_user` + `guest_credential` | **TC:** Hợp nhất quota nguyên tử (không cộng dồn), thu hồi credential khách cũ.<br>**TB:** Bỏ qua an toàn nếu credential không hợp lệ. | `test_guest_claim_and_quota_merge` | G03 / SD21 / AD17 |
| 4 | **Khám phá Tour & Bản đồ** | `MapScreen` / `TourList` | `GET /api/v1/tours`, `GET /api/v1/tours/{id}` | `tour_service.get_tour_detail` | `tour_repo` / `tours`, `POI` | Public (Khách hoặc User) | **TC:** Hiển thị danh mục tour, giá VND, nhãn trạng thái (Nghe thử / Đã mua), danh sách POI, tóm tắt.<br>**TB:** Empty state nếu chưa có tour. | `test_public_tour_exploration` | T12 / SD14 / AD14 |
| 5 | **Kiểm tra quyền nghe Tour** | `TourDetail` / `NarrationBar` | `GET /api/v1/tours/{id}/access` | `access_service.get_tour_access` | `entitlement_repo`, `trial_repo` / `tour_entitlements`, `trial_usage` | Subject hợp lệ (Guest hoặc User) | **TC:** Trả `can_preview`, `has_entitlement`, `trial_remaining`, `can_download`.<br>**TB:** Trả lý do `TRIAL_EXHAUSTED` hoặc `REQUIRES_AUTH`. | `test_tour_access_check` | T08 / SD22 / AD18 |
| 6 | **Chủ động Nghe Thử 1 POI (Trial)** | Nút `🎧 Nghe Thử Miễn Phí` trên POI Card | `POST /api/v1/playback-grants` | `access_service.request_playback_grant` | `trial_repo`, `playback_grant_repo` / `trial_usage`, `playback_grants` | Subject chưa mua + `trial_remaining > 0` + tour có `preview_enabled` | **TC:** Đặt trước quota nguyên tử, trả `grant_token` và `stream_url`, phát audio.<br>**TB:** Bật Paywall Modal nếu hết lượt. | `test_trial_playback_atomic_reservation` | G02 / SD22 / AD18 |
| 7 | **Tự động GPS trong vùng Geofence** | `GeofenceEngine` (Mobile/Web) | `POST /api/v1/playback-grants` | `access_service.request_playback_grant` | `trial_repo`, `entitlement_repo` / `trial_usage`, `tour_entitlements` | Đã mua tour (Entitled). **Nếu là Trial: bắt buộc người dùng xác nhận, không tự đốt lượt** | **TC:** Tự động phát thuyết minh nếu đã mua. Nếu là Trial: hiển thị thông báo hỏi người dùng có muốn dùng lượt nghe thử không.<br>**TB:** Bật Paywall nếu chưa mua và hết trial. | `test_gps_does_not_burn_trial_silently` | T09 / SD22 / AD18 |
| 8 | **Quét QR Địa Điểm (POI QR)** | `QRScanScreen` | `GET /api/v1/qr/{code}` + `POST /api/v1/playback-grants` | `qr_service.lookup`, `access_service` | `qr_repo`, `playback_grant_repo` | Tương tự luồng phát: Entitlement hoặc Trial hợp lệ | **TC:** Nhận diện đúng POI, cấp grant và phát âm thanh tương ứng.<br>**TB:** Báo lỗi QR không hợp lệ hoặc bật Paywall nếu hết lượt. | `test_qr_narration_access_check` | T11 / SD22 / AD18 |
| 9 | **Bật Paywall khi hết lượt** | `PaywallModal` | Frontend Access Guard | `access_service` | `trial_usage` | Khi `trial_remaining == 0` và `has_entitlement == False` | **TC:** Hiển thị thông tin tour, giá niêm yết VND, danh sách lợi ích khi mua, nút Mua Ngay.<br>**TB:** Không cho phát audio tiếp theo. | `test_paywall_display_after_trial` | P01 / SD22 / AD18 |
| 10 | **Tạo Đơn Hàng Mua Tour** | Nút `Mua Tour Ngay` trên Paywall/TourDetail | `POST /api/v1/orders` | `order_service.create_order` | `order_repo`, `tour_repo` / `orders`, `tours` | User đã đăng nhập (`admin_users` role `user` trở lên). Khách bị chặn yêu cầu login | **TC:** Đọc giá database, snapshot đơn hàng, trả `order_id`, trạng thái `pending_payment`.<br>**TB:** 401 nếu là khách; 409 nếu tour đã được sở hữu. | `test_order_creation_and_guest_blocked` | P01 / SD23 / AD19 |
| 11 | **Bắt đầu Thanh Toán (Attempt)** | Nút `Thanh Toán` trên Checkout Modal | `POST /api/v1/orders/{id}/payment-attempts` | `payment_service.create_payment_attempt` | `order_repo` / `payment_attempts` | Chủ đơn hàng (`user_id == actor_id`) | **TC (Visa):** Trả `checkout_url` VNPAY.<br>**TC (VietQR):** Trả mã VietQR động qua payOS.<br>**TB:** 400 nếu đơn không ở trạng thái `pending_payment`. | `test_payment_attempt_creation` | P02 / SD23 / AD19 |
| 12 | **Webhook / IPN Xác Minh Giao Dịch** | Background Webhook Listener | `POST /api/v1/payments/webhook/payos`<br>`GET /api/v1/payments/ipn/vnpay` | `payment_service.handle_provider_webhook` | `payment_event_repo`, `order_repo`, `entitlement_repo` / `payment_events`, `orders`, `tour_entitlements` | Chữ ký số HMAC hợp lệ của Provider | **TC:** Xử lý idempotent, chạy MongoDB transaction: attempt `succeeded`, order `paid`, cấp `tour_entitlements` `active`.<br>**TB:** 400 nếu sai chữ ký; ghi `requires_review` nếu sai số tiền. | `test_webhook_verification_and_settlement` | P03 / SD24 / AD19 |
| 13 | **Kiểm Tra & Đối Soát Đơn Hàng** | Nút `Tôi Đã Chuyển Khoản` / Auto-poll | `POST /api/v1/orders/{id}/reconcile` | `payment_service.reconcile_order` | `order_repo`, `entitlement_repo` / `orders`, `tour_entitlements` | Chủ đơn hàng (Rate-limited) | **TC:** Server query trực tiếp provider; nếu thành công cập nhật order & cấp entitlement.<br>**TB:** Trả trạng thái hiện hành `pending_payment` kèm hướng dẫn. | `test_reconcile_order_server_query` | P06 / SD24 / AD20 |
| 14 | **Xem Tour Đã Mua** | Tab `Tour Của Tôi` | `GET /api/v1/me/tours` | `entitlement_service.get_user_entitled_tours` | `entitlement_repo`, `tour_repo` / `tour_entitlements`, `tours` | User đã đăng nhập | **TC:** Liệt kê các tour đã mua, mở nghe toàn bộ POI không giới hạn, nút tải offline.<br>**TB:** Empty state nếu chưa mua tour nào. | `test_get_my_entitled_tours` | P04 / SD25 / AD18 |
| 15 | **Xem Lịch Sử Đơn Hàng** | Tab `Đơn Hàng Của Tôi` | `GET /api/v1/me/orders` | `order_service.get_user_orders` | `order_repo` / `orders` | User đã đăng nhập (Chống IDOR) | **TC:** Hiển thị danh sách đơn, giá, trạng thái, ngày tạo, chi tiết giao dịch.<br>**TB:** 401 nếu chưa đăng nhập; không xem được đơn người khác. | `test_my_orders_idor_protection` | P05 / SD23 / AD19 |
| 16 | **Tải Gói Tour Ngoại Tuyến** | Nút `Tải Gói Offline` | `POST /api/v1/tours/{id}/offline-pack` | `offline_package_service.build_tour_pack` | `entitlement_repo`, `offline_pack_manifests` | User sở hữu `tour_entitlements` `active` | **TC:** Cấp manifest dữ liệu, checksum, file media và `offline_license` có hạn 7 ngày.<br>**TB:** 403 `TOUR_NOT_ENTITLED` nếu chưa mua tour. | `test_offline_pack_requires_entitlement` | F02 / SD26 / AD21 |
| 17 | **Nghe Ngoại Tuyến & Kiểm Hạn** | `OfflineScreen` (Mobile) | Local Storage Engine | `OfflinePackService.verify_license` | Local Secure Storage | License còn hạn (<= 7 ngày kể từ lần online cuối) | **TC:** Phát thuyết minh từ bộ nhớ máy không cần 4G/Wifi.<br>**TB:** Báo license hết hạn, yêu cầu kết nối mạng để gia hạn. | `test_offline_license_expiration` | F06 / SD26 / AD21 |
| 18 | **Cấu Hình Giá Tour (Admin)** | Tab `Quản Lý Tour` (Admin CMS) | `PUT /api/v1/admin/tours/{id}/pricing` | `tour_service.update_tour_pricing` | `tour_repo` / `tours` | Admin / Super Admin (`tour:manage_pricing`) | **TC:** Cập nhật `price_amount`, `currency`, tăng `pricing_version`, bật `is_purchasable`.<br>**TB:** 403 Forbidden nếu không đủ quyền. | `test_admin_tour_pricing_configuration` | C15 / SD14 / AD14 |
| 19 | **Đối Soát & Xem Đơn Hàng (Admin)** | Tab `Giao Dịch & Đối Soát` (Admin CMS) | `GET /api/v1/admin/payments/orders`<br>`POST /api/v1/admin/payments/orders/{id}/reconcile` | `payment_service.admin_reconcile` | `order_repo`, `audit_logs` / `orders`, `audit_logs` | Admin / Super Admin (`payment:read`, `payment:reconcile`) | **TC:** Hiển thị danh sách đơn toàn hệ thống, đối soát trực tiếp cổng thanh toán, ghi log audit.<br>**TB:** Owner bị chặn truy cập. | `test_admin_reconciliation_and_owner_blocked` | P06 / SD24 / AD20 |
